# Backend API (Spring Boot). Leave empty to use same-origin / Vite proxy.
# When connecting directly, ensure the URL includes the /api prefix:
VITE_API_BASE_URL=http://localhost:8080/api

# Optional Gemini AI (if used by the UI)
# GEMINI_API_KEY=
# APP_URL=
